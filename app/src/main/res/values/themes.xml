<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QTAAndroid" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.QTAAndroid" parent="Base.Theme.QTAAndroid" />

    <style name="MyTextInputLayout" >
        <!-- border color -->
        <item name="boxStrokeColor">@color/mtrl_textinput_default_box_stroke_color</item>

        <!-- The color of the label when it is collapsed and the text field is active -->
        <!-- The color of the label in all other text field states (such as resting and disabled) -->
        <item name="hintTextColor">@color/mtrl_textinput_default_box_stroke_color</item>
        <item name="android:textColorHint">@color/mtrl_textinput_default_box_stroke_color</item>
    </style>
</resources>